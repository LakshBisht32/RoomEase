<%- include('../partials/head', { title: 'Listing' }) %>
<%- include('../partials/navbar', { user: user }) %>
<main class="page" id="listing-page" data-listing-id="<%= listingId %>" data-logged-in="<%= user ? '1' : '' %>" data-user-role="<%= user ? user.role : '' %>">
  <div id="listing-content"><p>Loading…</p></div>
</main>
<%- include('../partials/footer', { pageScript: '/js/listing-detail.js' }) %>
